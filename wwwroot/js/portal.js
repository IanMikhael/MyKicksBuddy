(() => { const side=document.querySelector('[data-portal-sidebar]'); const overlay=document.querySelector('[data-portal-overlay]'); const set=open=>{side?.classList.toggle('is-open',open); if(overlay){overlay.hidden=!open; overlay.classList.toggle('is-open',open);} document.body.classList.toggle('portal-nav-open',open);}; document.querySelector('[data-portal-open]')?.addEventListener('click',()=>set(true)); document.querySelector('[data-portal-close]')?.addEventListener('click',()=>set(false)); overlay?.addEventListener('click',()=>set(false)); })();
